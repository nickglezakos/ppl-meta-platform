# PPL Meta Platform - Complete Ecosystem
# This orchestrates all microservices in the platform

# Include the infrastructure from the node service
include:
  - path: ./ppl-meta-node/docker-compose.infrastructure.yml

services:
  # All services are defined in the included infrastructure file
  # This placeholder ensures valid compose structure
  placeholder:
    image: hello-world
    profiles:
      - never-run
# Networks are defined in the infrastructure file
