# PPL Meta Platform - LEGACY Docker Compose
# ⚠️  DEPRECATED: This file is legacy and no longer used
# Use the docker-compose files in the root directory instead:
# - docker-compose.minimal.yml (for basic services)
# - docker-compose.ecosystem.yml (for full ecosystem)
#
# This file is kept for reference only

version: "3.8"

services:
  # ⚠️  ALL SERVICES HAVE BEEN MOVED TO ROOT-LEVEL DIRECTORIES
  #
  # Active microservices are now located at:
  # - /ppl-meta-gateway     (API Gateway)
  # - /ppl-meta-node        (User Management)
  # - /ppl-meta-media       (Media Service)
  # - /ppl-meta-orchestrator (Orchestrator)
  #
  # Use these Docker Compose files from the root directory:
  # - docker-compose.minimal.yml    (Core services only)
  # - docker-compose.ecosystem.yml  (Full ecosystem)
  #
  # This file is kept for reference only

  # Load balancer and reverse proxy (LEGACY)
  # nginx-gateway:
  #   build: ./infrastructure/nginx
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   volumes:
  #     - ./infrastructure/nginx/conf:/etc/nginx/conf.d
  #     - ./infrastructure/nginx/ssl:/etc/ssl
  #   depends_on:
  #     - api-gateway
  #     - user-management
  #     - media-service
  #   networks:
  #     - ppl-public
  #     - ppl-internal

  # API Gateway service (LEGACY - now at /ppl-meta-gateway)
  # api-gateway:
  #   build: ./services/gateway  # ❌ Directory removed
  #   ports:
  #     - "8080:8080"
  #   environment:
  #     - SERVICE_NAME=api-gateway
  #     - CONSUL_HOST=consul
  #   depends_on:
  #     - consul
  #     - redis
  #   networks:
  #     - ppl-internal
  #   volumes:
  #     - ./shared:/app/shared

  # User Management service (LEGACY - now at /ppl-meta-node)
  # user-management:
  #   build: ./services/user-management  # ❌ Directory removed
  #   ports:
  #     - "8001:8001"
  #   environment:
  #     - DATABASE_URL=postgresql://admin:password@postgres:5432/user_db
  #   depends_on:
  #     - postgres
  #   networks:
  #     - ppl-internal
  #   volumes:
  #     - ./shared:/app/shared

  # Media service (LEGACY - now at /ppl-meta-media)
  # media-service:
  #   build: ./services/media  # ❌ Directory removed
  #   ports:
  #     - "8000:8000"
  #   environment:
  #     - DATABASE_URL=postgresql://admin:password@postgres:5432/media_db
  #   depends_on:
  #     - postgres
  #   networks:
  #     - ppl-internal
  #   volumes:
  #     - ./shared:/app/shared
  #     - media-storage:/app/media

  # Orchestrator service (LEGACY - now at /ppl-meta-orchestrator)
  # orchestrator:
  #   build: ./services/orchestrator  # ❌ Directory removed
  #   ports:
  #     - "8002:8002"
  #   environment:
  #     - DATABASE_URL=postgresql://admin:password@postgres:5432/orchestrator_db
  #   depends_on:
  #     - postgres
  #     - user-management
  #     - media-service
  #   networks:
  #     - ppl-internal
  #   volumes:
  #     - ./shared:/app/shared

  # Database (LEGACY - use infrastructure services from root docker-compose files)
  # postgres:
  #   image: postgres:15-alpine
  #   environment:
  #     POSTGRES_DB: ppl_platform
  #     POSTGRES_USER: admin
  #     POSTGRES_PASSWORD: password
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #     - ./infrastructure/database/init:/docker-entrypoint-initdb.d
  #   networks:
  #     - ppl-internal

  # Redis for caching and sessions (LEGACY)
  # redis:
  #   image: redis:7-alpine
  #   networks:
  #     - ppl-internal
  #   volumes:
  #     - redis_data:/data

  # Service discovery (LEGACY)
  # consul:
  #   image: consul:1.16
  #   ports:
  #     - "8500:8500"
  #   networks:
  #     - ppl-internal
  #   volumes:
  #     - consul_data:/consul/data

  # Monitoring (LEGACY)
  # prometheus:
  #   image: prom/prometheus:latest
  #   ports:
  #     - "9090:9090"
  #   volumes:
  #     - ./infrastructure/monitoring/prometheus.yml:/etc/prometheus/prometheus.yml
  #   networks:
  #     - ppl-internal

  # grafana:
  #   image: grafana/grafana:latest
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     - GF_SECURITY_ADMIN_PASSWORD=admin
  #   networks:
  #     - ppl-internal

# networks:
#   ppl-public:
#     driver: bridge
#   ppl-internal:
#     driver: bridge

# volumes:
#   postgres_data:
#   redis_data:
#   consul_data:
#   media-storage:

networks:
  ppl-public:
    driver: bridge
  ppl-internal:
    driver: bridge
    internal: false
