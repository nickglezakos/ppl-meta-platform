# Lab SQL leftovers

- `012_*` / `014_*` — still used (ORM clash fixes); included in the schema pack via sync.
- `015*` — optional lab data cleanup only.
- `archive-stubs/` — old minimal schemas; **do not apply**. Use `sync-schema-pack.sh` + `schema/apply.sh` instead.
