# Flutter Frontend - Dockerfile for Web Deployment

FROM nginx:alpine

# Copy built Flutter web app
COPY build/web /usr/share/nginx/html

# Copy custom nginx configuration
COPY nginx.conf /etc/nginx/nginx.conf

# Expose port 80
EXPOSE 80

# Start nginx
CMD ["nginx", "-g", "daemon off;"]
